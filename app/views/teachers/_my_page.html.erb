<% profile = teacher.profile %>
<div class="medium float-left clear">
	<h2>My Resources </h2>
    <table>
        <% if !readable_branches.empty? || !writable_branches.empty? %>
	        <tr>
	        	<td>Branches Managed (Accessible)</td>
	            <td> 
            		<% readable_branches.each do |rbr| %>
            			<div class='readable'>
            				<%= link_to rbr.name, branch_path(rsec)%>, &nbsp; <%= link_to rbr.institution.name, institution_path(rbr.institution)%> <br>
            			</div>
            		<% end%>
            		<br>
            		<% writable_branches.each do |wbr| %>
            			<div class='writable'>
            				<%= link_to wbr.name, branch_path(wbr)%>, &nbsp; <%= link_to wbr.institution.name, institution_path(wbr.institution)%> <br>
            			</div>
            		<% end%>      		
	        	</td>
	        </tr>            
        <% end %>    	
        <tr>
        	<td>Classes owned (class teacher)</td>
            <td> 
            	<% sec = Section. where('class_teacher_id = ?', teacher.id).all %>
            	<% if !sec.empty? %>
            		<% sec.each do |s| %>
            			<%= link_to s.name, section_path(s) %>, &nbsp; <%= link_to s.clazz.name, clazz_path(s.clazz) %> <br>
            		<% end %>
            	<% else %>
            		NA
            	<% end %>            	
        	</td>
        </tr>    
        <tr>
        	
        <tr>
        	<td>Classes Handled (taking subs)</td>
            <td> 
            		<% teacher.sec_sub_maps.each do |m| %>
            			<%= link_to m.section.name, section_path(m.section) %>, &nbsp; <%= link_to m.section.clazz.name, clazz_path(m.section.clazz)%> &nbsp; (<%= m.subject.name %>)<br>
            		<% end %>
            </td>
        </tr> 
        
        <tr>
        	<td>Classes Managed (Accessible)</td>
            <td> 
            	<% if readable_sections.empty? && writable_sections.empty? %>
            		NA
            	<% else %>
            		<% readable_sections.each do |rsec| %>
            			<div class='readable'>
            				<%= link_to rsec.name, section_path(rsec)%>, &nbsp; <%= link_to rsec.clazz.name, clazz_path(rsec.clazz)%>
            			</div>
            		<% end%>
            		<br>
            		<% writable_sections.each do |wsec| %>
            			<div class='writable'>
            				<%= link_to wsec.name, section_path(wsec)%>, &nbsp; <%= link_to wsec.clazz.name, clazz_path(wsec.clazz)%>
            			</div>
            		<% end%>            		
            	<% end %>            	
        	</td>
        </tr>  
    </table>          
</div>

<div class="medium float-left">
	<h2>Today's Reminders/Notifications</h2>	
	1. Industrial visit requested by the 1st year Mechanical class<br>
	2. Teacher's meeting at 11 am today <br>
	3. Lab practicals for X std at 2:30 pm <br>
	4.  Meeting with contractors at 5 pm<br>
</div>

<div class="small float-left clear">
	<h2>This Week's Events</h2>
	<ul class="no-list-style">
		<li> <b>Computer lab inspection happens today</b> </li>	
		<li>Quarterly Exams for X std start on <%= (Date.today + 2).to_s(:long) %></li>
		<li> Farewell to Rajeshwaran on <%= (Date.today + 4).to_s(:long)%></li>					
		<li> Parents-Teachers meet on <%= (Date.today + 4).to_s(:long)%></li>							
	</ul>	
</div>

<div class="small float-left">
	<h2>To-do's </h2>	
	<ul class="no-list-style">
		<li> <b>Process the mark lists of 10th std, due: today</b> </li>	
		<li>Send weekly invite meeting to teachers, due: <%= (Date.today + 3).to_s(:long) %></li>
	</ul>	
</div>

<div class="small float-left">
	<h2>Who's missing today?</h2>	
	<h3> Teachers </h3>
	<ul class="no-list-style">
		<li> Rajesh Sundaram </li>	
		<li>Sundari Ramesh</li>
		<li>Karthick Santhanam </li>
	</ul>		
	<br>
	<h3> Students </h3>
	<ul class="no-list-style">
		<li> Dharshini Rajashekar </li>	
		<li> Mukesh Raj</li>
		<li> Monish SivaKumar </li>
		<li> Pranav Ram </li>		
	</ul>	
</div>


